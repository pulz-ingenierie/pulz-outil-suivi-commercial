/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // La config sensible vit en variables d'environnement (Vercel) — rien en dur ici.
};

export default nextConfig;
